export const stakeUrl = () => {
  return `/staking`
}

export const uploadUrl = () => {
  return `/upload`
}

export const resellUrl = () => {
  return `/resell`
}

export const marketUrl = () => {
  return `/resell`
}

export const navLinks = {
stakeUrl,
uploadUrl,
resellUrl,
marketUrl
}
